# AxoBoard state

Updated: 2026-08-17

## Current phase

Combined visualization-family and workspace-isolation release candidate. Twelve display types now use bounded snapshot payloads, while dashboard layout, deletion, and TV mode are driven only by the authenticated workspace's PostgreSQL state.

Production URL: `https://axoboard.io`

Current release branch: `fix/workspace-layout-tv-data`

Current production commit before this release: `424bd3aff752969babc2fb0c486ec979d99e80aa`

Standing business goal: at least $5,000 durable MRR, with $6,000 MRR as the operating target for margin and owner-income buffer. All roadmap decisions must be evaluated against repeatable paid adoption, 80%+ gross margin, minimal custom work, and reliable customer outcomes.

## Release candidate changes

- Added Goal Pace and Gauge scalar cards using one prepared numeric cell and an optional goal; Gauge safely derives a round maximum when no goal exists.
- Added Trend, Category Bar, Funnel, and Pipeline cards for ordered label/value ranges with optional same-shape comparisons.
- Added Activity Feed for 2–4 prepared event columns and Heatmap for labeled numeric matrices.
- Added migration `007_visualization_families.sql`; the persisted allowlist now covers 12 total display types.
- Added data-shape requirements, builder help, automatic comparison disabling where unsupported, and 12 direct-owned display controls.
- Added responsive renderers with escaped customer content, bounded rows/cells, mobile internal scrolling, and no chart-library dependency.
- Fixed live KPI cards remaining hidden behind the blank-customer state when an authenticated workspace still had the prototype `sample-empty` browser key.
- Hardened the billing test so an early child-server exit reports logs instead of waiting forever on an already-fired exit event.
- Added PostgreSQL-backed, tenant-filtered dashboard layout/order, real KPI drag/reorder controls, workspace-scoped soft deletion, and live TV data.
- Removed the authenticated fake ACME switch and all hardcoded sample KPI values from TV mode.
- Preview shape mismatches now return an explicit HTTP 200 validation result, keeping the builder's guidance while eliminating repeated failed-resource 422 console noise. KPI creation remains strict and returns 422 for invalid selections.
- Renumbered the dashboard migration to `008_workspace_dashboard_layout.sql` so it follows visualization migration `007` deterministically.

## Release candidate verification

- PostgreSQL-backed `npm run verify`: passed across smoke, Stripe billing, Google OAuth/range/sync, tenant isolation, and visualization payload contracts.
- Dependency audit: zero vulnerabilities.
- Desktop 1440×1000: all eight new cards visible, no document overflow, no card overflow, no browser/console errors.
- Mobile 390×844: all eight new cards visible, no document overflow, and intentional inner scrolling only for wide pipeline/heatmap data.
- Builder mobile QA: 12 display choices, zero controls below 44px, zero modal overflow, and no generic-workflow hijack.
- QA captures: `/tmp/axoboard-visualizations-desktop.png`, `/tmp/axoboard-visualizations-mobile.png`, and `/tmp/axoboard-visualization-builder-mobile.png`.
- Rebased workspace/layout work onto visualization commit `424bd3a`; PostgreSQL-backed full verification passed again, including preview-valid/preview-invalid contracts and strict create rejection.

## Blocking next work

1. Create the AxoBoard production Google OAuth web client, enable Sheets API, register `https://axoboard.io/api/integrations/oauth/google/callback`, and add the four Google/encryption Railway variables.
2. Deploy `feat/google-sheets-kpi`, run fresh Google consent with the retained QA tenant, then create one KPI from a test spreadsheet and verify scheduled refresh/revocation.
3. Add dashboard draft/version/publish/rollback semantics; the current saved layout is workspace-scoped but intentionally single-version.

## PostgreSQL production proof

- Railway `Postgres` is attached through `DATABASE_URL`; `/healthz` reports `database: healthy`.
- Duplicate `Postgres-U7Hj` was removed and its detached volume was queued for permanent deletion; the correct `postgres-volume` remains attached and ready.
- Synthetic Stripe-ready fixture: `qa+postgres-20260813-01@axoboard.io`, workspace `AxoBoard Production QA`, workspace ID `5bcdfc3c-a7ef-4e56-bfc7-29a627775f32`.
- Production signup, secure session cookie, session lookup, invalid/valid login, logout invalidation, pending-payment `/app` redirect, and protected asset denial passed.
- Restarted only the AxoBoard web service; the same account, workspace, membership, entitlement, and login survived, proving PostgreSQL persistence rather than process-memory state.
- Test credentials are stored outside Git at `/home/ops/.openclaw/workspace-murphy/.credentials/axoboard-production-test.env` for Stripe sandbox verification.

## Stripe sandbox foundation

- Official Stripe Node SDK pinned at `22.5.0` with API version `2025-12-15.clover`.
- Added authenticated Starter Checkout and customer portal endpoints; only the server-side `STRIPE_PRICE_STARTER_MONTHLY` allowlist is accepted.
- Added raw-body signature verification, unique event IDs, transactional processing, mode matching, monotonic Stripe event timestamps, allowed-plan/price validation, and customer/subscription cross-workspace checks.
- Checkout completion binds trusted Stripe IDs but does not activate access; subscription/invoice webhooks are the entitlement authority.
- Added `002_stripe_billing.sql` for Stripe event audit records and subscription projection fields.
- Starter CTA now routes authenticated pending-payment workspaces to Stripe-hosted Checkout; anonymous users still enter signup and active workspaces open the app.
- Deterministic Stripe double covers Checkout fail-closed behavior, signed/invalid signatures, duplicate delivery, out-of-order delivery, payment failure revocation, customer portal, and tenant isolation.
- Local gates passed against disposable PostgreSQL 18: `npm run verify`, zero dependency vulnerabilities, production Docker build, Git diff check, and Gitleaks with no findings.

## Google Sheets one-KPI vertical slice

- Added `003_google_sheets_kpi.sql` for expiring OAuth transactions, encrypted tenant-owned connections, KPI mappings, immutable metric snapshots, and observable sync runs.
- Added one-time SHA-256 state digests, PKCE S256, ten-minute expiry, exact session/workspace binding, server-side code exchange, verified OpenID identity, and AES-256-GCM token envelopes with tenant/connection AAD.
- Added access-token refresh, retry/backoff capped at three for safe reads, spreadsheet metadata, worksheet selection, exact cell/range preview, seven deterministic aggregations, KPI save/list/manual sync, scheduled five-minute refresh, last-known-good preservation, and Google revocation on disconnect.
- Browser flow now displays the real connected account, accepts a spreadsheet URL/ID, loads worksheets, previews exact lineage/value, persists the KPI, and renders the live card.
- PostgreSQL 18 integration proof covers one-time OAuth replay denial, encrypted token storage, refresh, Sheets metadata/range calculation, persisted snapshots, scheduled sync, disconnect, and swapped-tenant IDs returning `403` with zero provider calls.
- Browser QA passed the complete connected-account -> range preview -> saved KPI flow. Mobile 390x844 passed with zero document overflow and zero undersized visible builder controls.

## Historical deliverables and prototype inventory

- Public landing page with product positioning, live dashboard preview, product surfaces, integration catalog, pricing bridge, FAQ, and conversion paths.
- Responsive login and three-step signup experience with transactional user/workspace creation.
- PostgreSQL auth foundation with scrypt password hashing, hashed sessions, HttpOnly/SameSite cookies, rate limits, origin validation, and tenant-owner membership creation.
- Public demo access removed; anonymous app requests redirect to login and product assets fail closed.
- Low-poly axolotl favicon standardized across public, auth, demo, and app surfaces.
- GitHub Actions release gate for syntax, dependency audit, PostgreSQL auth smoke tests, sensitive-path probes, Gitleaks, and Docker build.
- Production verification script and release/rollback runbook.
- Browser bundle sanitized of Murphy Door names, credentials, and tenant-specific references; independent Gitleaks history/worktree scans report zero findings.

- Separate AxoBoard repository and product boundary.
- Market analysis and technical blueprint.
- Customization product specification with celebration and Kombat contracts.
- Leucistic-axolotl design system and three generated visual references.
- Aquatic light-blue secondary palette and restrained GFP fluorescence styling across the product shell, live states, celebrations, sound library, Kombat arena, and brand preview.
- Custom KPI dashboard with metric, goal, status, sparkline, trend, freshness, source-lineage, and action-center examples.
- Integration catalog and interactive three-step KPI builder for Google Sheets cells/ranges and HubSpot standard/custom properties.
- AxoBoard brand guide, integration/OAuth specification, and Geckoboard competitive roadmap.
- Interactive TV/display management, dashboard loops, outcome recipes, secure sharing/snapshots, KPI drilldowns, and KPI-driven automation rules.
- Production contracts for displays, loops, recipes, share grants, snapshot schedules, automation rules/runs, and metric lineage.
- Mobile usage worksheet, route acceptance matrix, weak-network recovery cases, and a mandatory mobile release gate.
- Control-level interaction audit plus reusable workflow wireframes for every previously silent or shallow feature control.
- Customer-facing Workspace Admin for onboarding, roles, usage, billing entry points, service health, and support.
- Commercial SaaS blueprint covering tenant isolation, permissions, entitlements, billing, onboarding, support, launch gates, and cost controls.
- Browser-persistent, non-sensitive beta drafts for workspace identity and key customization choices.
- Blank example-customer tenant with zero inherited data/assets/connections, repeatable reset, explicitly synthetic KPI fixtures, and fresh provider OAuth handoff wireframes.
- Direct provider OAuth selected to avoid connector-platform fees, with an AxoBoard-owned connection contract and Google-first implementation sequence.
- Interactive Celebration HQ, My Sounds, Team Competitions, and Brand Studio wireframes.
- Desktop and mobile QA screenshots.
- Transparent low-poly leucistic axolotl primary logo and retained generation source.
- Restored Fredoka 700 AxoBoard wordmark; the custom angular vector is retired and no longer installed in the beta navigation.
- Recognizable Google Sheets and HubSpot provider marks with a reusable integration-asset standard and provenance/trademark registry.
- Provider-neutral direct OAuth foundation under `/api/axoboard/integrations/oauth/*`: authenticated start, Google callback, one-time state, PKCE S256, server-side exchange, OpenID account verification, and AES-256-GCM token storage.
- Production hosting plan for `axoboard.io`, including separate app/API origins, managed PostgreSQL, signed Nango webhooks, workers, backups, health checks, and rollback.
- Persistent internal-network beta preview through `axoboard-beta.service`.
- Leroy-only remote preview through the existing Murphy Dashboards login and ngrok domain.
- Standalone Node service, Docker image, and Railway deployment contract with `/healthz`, graceful shutdown, non-root runtime, and no Murphy Dashboard dependency.
- Customer-facing Plans & Billing wireframe with monthly/annual switching, exact Starter/Growth/Scale/Enterprise feature gates, capacity limits, and responsive comparison table.
- Commercial pricing and $5K MRR plan at `docs/PRICING_AND_REVENUE_MODEL.md`, including the 24-customer durable target, beta/production infrastructure budget, margin guardrails, and design-partner bridge.

## Verification

- `node --check wireframes/app.js`: passed.
- DOM contract: 129 unique IDs; all 100 direct scripted ID selectors resolve; no duplicates or missing selectors.
- All eight routes activate correctly: Dashboard, Integrations, Displays, Automations, Celebration HQ, My Sounds, Kombat Studio, and Brand Studio.
- Recipes, sharing tabs, scheduled snapshots, drilldowns, TV countdown, screen controls, loop reordering, rule toggles, rule draft creation, and modal Escape/close behavior: passed.
- Mobile route captures: all eight surfaces at 390×844 plus templates, share, drilldown, TV preview, KPI builder, and celebration modals.
- Required viewport matrix at 320×568, 390×844, 430×932, 768×1024, and 1024×768: zero document-level overflow on every route.
- 390px touch-target audit: zero visible route or feature-modal buttons below the 44×44px gate.
- Browser page errors after parity-plus and mobile hardening pass: none.
- Interaction audit: 182 initial buttons; 98 working, 84 feature-wireframed, 0 silent/unclassified.
- Current blank-tenant build: 195 buttons; 111 working, 84 feature-wireframed, 0 silent/unclassified.
- Representative interactions across all 20 workflow families and multi-step advancement: passed on desktop and mobile.
- Workspace Admin and all nine product routes: zero document overflow at 320×568, 390×844, 430×932, 768×1024, and 1024×768.
- Customer onboarding draft persistence across browser refresh: passed.
- Murphy Door defaults to zero KPIs and zero connections; synthetic KPI state is visibly labeled, persists on refresh, and resets to blank: passed.
- Fresh Google then HubSpot OAuth entry creates attempts 1 and 2 without provider redirect, credential access, or token storage: passed.
- Workspace switch Murphy Door -> Acme Sales preserves blank/populated tenant separation and generic Murphy game defaults: passed.
- Murphy sandbox routes at 390×844: zero document overflow across all nine routes; OAuth modal screenshot captured.
- Murphy sandbox release matrix at 320×568, 390×844, 430×932, 768×1024, and 1024×768: zero document overflow on all nine routes; zero undersized active mobile controls.
- `git diff --check`: passed.
- Google Sheets and HubSpot integration SVGs parse successfully and return `200` from the beta asset server.
- Plans & Billing route passes desktop and 390×844 visual QA; mobile has zero document overflow, zero visible touch targets below 44×44px, and automatically scrolls the active nav item into view.
- Monthly/annual interaction test passes: $99/$249/$599 changes to $79/$199/$499 with annual billing labels; browser reported no page errors.
- Desktop and 390px integration/wordmark visual QA: passed.
- Direct OAuth security tests cover encryption-key validation, AES-GCM authenticated encryption, wrong-key rejection, and secret-free public connection payloads.
- Remote beta access: anonymous `401`, non-Leroy authenticated user `403`, Leroy session `200`, protected asset `200`.
- Remote preview cookie: short-lived, `HttpOnly`, `Secure` over ngrok HTTPS, and `SameSite=Strict`; it is scoped to the shared host so the isolated `/api/axoboard/*` bridge can validate the beta session.

## Product decisions

- AxoBoard focuses on customization, celebrations, ease of use, My Sounds, and branded team competition.
- Launch pricing hypothesis: Starter $99/$79 annual equivalent, Growth $249/$199, Scale $599/$499, and Enterprise from $1,500; unlimited dashboards and viewers on all paid plans.
- Durable $5K MRR target: 5 Starter, 16 Growth, and 3 Scale customers at annual-equivalent pricing = $5,076 MRR.
- Leucistic pink and warm white remain dominant; aquatic blue supports navigation/depth, while GFP green is reserved for live, success, momentum, and rare glow moments.
- Murphy Kombat is a configurable preset, not a hardcoded AxoBoard feature identity.
- Customer assets and branding remain tenant-scoped and versioned.
- Scoring events must be deterministic, idempotent, traceable, and replay-safe.
- Celebration controls require quiet hours, rate limits, mute controls, and reduced-motion support.

## Known limits

- Stripe sandbox billing is production-active; live-mode keys, tax registration, and automatic tax remain intentionally disabled.
- The GitHub repository is publicly readable. Automated secret scans pass, but changing visibility requires owner approval.
- Dashboard layout persists in workspace-scoped PostgreSQL; draft/publish/version/rollback history is not implemented yet.
- Dashboard audio, publishing, display, automation, Team Competitions, and uploads remain high-fidelity prototypes without workers or asset storage/scanning.
- Google Sheets and visualization code is deployed and production-configured; this combined release still requires post-deploy interactive verification against the retained QA tenant.
- Public integration marks remain compatibility illustrations with roadmap status; provider-issued assets/partner approvals must be cleared before representing a connector as live.
- Google Fonts are loaded remotely and should be self-hosted before paid ad scale.
- Automated mobile QA uses Chromium; physical Safari/iOS VoiceOver and Android TalkBack remain release gates.

## Next phase

1. Create/configure the AxoBoard production Google OAuth client and a new production encryption key.
2. Deploy this branch and complete fresh consent plus one test-spreadsheet KPI in the retained QA workspace.
3. Persist dashboard drafts/layout versions and implement publish/rollback in PostgreSQL.
4. Add HubSpot, Shopify, and Excel only after the real Google consent/revocation gates pass.
5. Implement the display/automation/celebration/Team Competitions workers behind published metric events.

## Rollback

The previous discovery prototype is preserved in Git commit `9fea28d`. Restore individual prototype files from that commit if needed.

For remote beta access, remove the AxoBoard route registration and `/app/axo-beta` client route from the sales-dashboard project, rebuild the client, and restart `sales-dashboard.service`.

## 2026-08-13 — Recent-first Google spreadsheet picker

- Feature branch: `feat/recent-google-sheets`
- Implementation commit: `fe73ef4260ae9df676f595c4cb01448c5d6fbe06`
- Added paginated Google Drive `files.list` discovery for native spreadsheets, ordered by `modifiedTime desc` and filtered to non-trashed spreadsheet files.
- Added the minimum `drive.metadata.readonly` OAuth scope; cell contents remain protected by `spreadsheets.readonly`.
- Replaced manual spreadsheet URL/ID entry with a recent-first spreadsheet selector that automatically loads the selected spreadsheet's sheets.
- Standardized customer-facing Google terminology to `spreadsheet` for the document and `sheet` for a tab inside it.
- Existing Sheets-only connections remain valid for current KPI synchronization but must reconnect before spreadsheet discovery is available.
- Verification passed: PostgreSQL auth, Stripe regression, paginated Drive discovery, sheet/range KPI, token encryption/refresh, cross-tenant zero-call isolation, Docker build, changed-file and 19-commit secret scans, desktop/mobile visual QA, and GitHub Actions run `31773881909`.
- Production remains on `88f6634`; do not promote until Google Drive API is enabled and `drive.metadata.readonly` is added to the AxoBoard OAuth app's Data Access configuration.
- Rollback: do not merge the feature branch, or revert the final promotion commit if it is later merged.

## 2026-08-14 — Release workflow optimization

- Added guarded `npm run release:check` and `npm run release` commands for exact-SHA feature CI validation, non-force fast-forward promotion, main-CI waiting, Railway exact-SHA waiting, and production smoke verification.
- Split GitHub CI into parallel PostgreSQL test, full-history secret scan, and cached Docker image jobs.
- Replaced repeated all-gates-local validation with a risk-tiered local matrix; authoritative remote gates remain unchanged.
- Full PostgreSQL 18 auth, Stripe, Google, syntax, and dependency verification passed after the workflow change.

## 2026-08-17 — Workspace-scoped dashboard layout and TV data fix

- Branch: `fix/workspace-layout-tv-data`, rebased onto `origin/main` at `424bd3a`.
- Confirmed KPI creation was already database-scoped by the authenticated session; the apparent ACME leak came from the browser-persisted prototype workspace being rendered before/alongside live state.
- Added workspace-scoped dashboard settings in PostgreSQL with validated KPI UUID ordering. Foreign-workspace IDs are discarded server-side.
- Live layout editor now supports real KPI cards, drag-and-drop, keyboard-safe move controls, Compact/KPI focus/Balanced presets, Cancel restore, and server persistence across refresh.
- Added guarded KPI deletion from cards and the layout editor. Deletion is workspace-scoped and soft-deletes the mapping while retaining metric history.
- Removed fake ACME workspace switching from authenticated product behavior; the workspace control now opens the bound workspace admin surface.
- Rebuilt TV mode from the current workspace's real KPI payload and saved order; removed all hardcoded sample values and the fake trend chart.
- PostgreSQL verification passed for auth, billing, Google, layout persistence, foreign-ID filtering, cross-tenant delete denial, and soft deletion.
- Browser QA passed at desktop and 390×844: zero horizontal overflow, 52px mobile reorder/delete controls, saved order survives reload, Cancel restores, TV contains live workspace values, and no page/console errors.
- QA artifacts: `artifacts/qa/workspace-tv-live-data.png`, `artifacts/qa/workspace-layout-mobile.png`, and `artifacts/qa/workspace-layout-mobile-order.png`.
- Preview validation now returns a successful validation envelope instead of expected 422 transport failures; invalid KPI creation still fails closed with 422.
- Deployment status: release verification in progress; no production promotion recorded yet.

## 2026-08-17 — White-label engagement wireframe

- Branch: `feat/white-label-engagement-wireframes`, based on `origin/main` at `aa58c896b3337a1a59ae930515123ded9050e855`.
- Added an interaction-first wireframe and product contract for certified metrics, Goal Pace/Gauge intelligence, resilient white-label TV runtime, an immutable celebration event ledger, and a generic competition scoring engine.
- Customer identity is resolved from the active workspace brand package across runtime metadata, ledgers, previews, asset naming, competitions, and offline/fallback coverage. AxoBoard identity remains in authenticated admin chrome.
- Desktop 1440×1000 and mobile 390×844 visual QA passed with zero document overflow; the ledger uses contained horizontal scrolling and new mobile controls are at least 45px high.
- `npm run verify` passed against disposable PostgreSQL, covering syntax/audit, auth smoke tests, Stripe billing isolation, and Google integration isolation.
- Status: wireframe only; no production runtime workers, score writes, event delivery, or release promotion.
